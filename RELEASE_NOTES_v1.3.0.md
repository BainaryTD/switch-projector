# Projector Control v1.3.0: Broadcast Console Edition

🎛️ **Projector Control Update (v1.3.0) - Broadcast Console Edition**
This release is a complete visual and layout overhaul of the Control Panel. Every feature from v1.2.0 is still here — but the panel now looks and behaves like a real broadcast switcher: color-coded states, always-reachable controls, and a layout that keeps your eyes where they matter during a live show. 🎬

## ✨ What's New

- **Brand New Console Layout:** The whole panel has been restructured. The **Media Queue is now a fixed left sidebar** that stays visible while you scroll, and the Hold / Live controls sit in a clean two-column grid on the right. No more scrolling up and down to find your queue mid-show.
- **Sticky Header with Target Screen:** The display selector moved into a **sticky top bar** — switch your projector output at any time without scrolling back to the top. The header also shows a live reminder that `?` opens the shortcut list.
- **Compact Sticky Emergency Bar:** The `🛑 Emergency Cut to Standby` button now lives in a **slim bar pinned to the bottom of the window** — always one click away, but no longer eating half the screen. The `B` hotkey is printed right on the button.
- **Color-Coded Hold vs. Live:** Hold / Standby is now framed in **amber**, Live Controls in **red**. One glance tells you which side you're touching — a big deal when you're working fast under pressure.
- **Pulsing LIVE & ARMED Indicators:** The `● LIVE`, `● ON AIR`, and `● ARMED` badges now **pulse softly**, and the Live Preview frame has a **red glow ring** around it. You can feel what's on air from across the room.
- **Integrated Live Player Frame:** The Keep Audio / Loop / Fade options now sit in a bar **on top of** the live player, and the seek + volume transport is docked **at the bottom of the same frame**. Everything about the live clip is in a single, self-contained panel.
- **Better Live Preview Sizing:** The Live Preview no longer forces a fixed 16:9 block that pushes content off-screen. It now scales to your window height (with sensible min/max limits) so the whole console fits on one screen.
- **Refined Drop Zone:** The drag & drop overlay is bigger and clearer, with a blurred backdrop and a **list of all supported file formats** shown right in the overlay — no guessing what the app accepts.
- **Polished Details Everywhere:** Deep gradient console background, unified card styling with soft shadows, gradient buttons with press feedback, **slim dark scrollbars**, and a redesigned keyboard shortcut panel with aligned key labels.
- **Keyboard Accessibility:** Buttons, dropdowns, and inputs now show a clear **blue focus ring** when tabbed to — the panel is fully navigable without a mouse.
- **Friendlier Empty State:** The empty queue now tells you what to do — *"Queue is empty — drag files here or click Add"*.

> ℹ️ **Note:** This is a UI/UX release. All controls, shortcuts, and behavior from v1.2.0 work exactly as before — nothing was removed or remapped.

## 📦 Installation & Usage
This release remains a Portable Windows Build. No installation required!

1. Download the `Projector.Control-win32-x64.rar` file below.
2. Extract the `.rar` file to your desired folder.
3. Open the folder and double-click `Projector Control.exe` to run the application.

> **Note:** This application requires Windows 64-bit to run.

---

## 🇹🇭 Thai Version

🎛️ **Projector Control (v1.3.0) - อัปเดตโฉมใหม่สไตล์ห้องคุมงานถ่ายทอดสด**
เวอร์ชันนี้คือการยกเครื่องหน้าตาและการจัดวางของ Control Panel ใหม่ทั้งหมด ฟีเจอร์จาก v1.2.0 ยังอยู่ครบทุกตัว แต่หน้าตาใหม่ทำให้ใช้งานเหมือนเครื่องสวิตช์เชอร์จริง ๆ — แยกสีตามสถานะ ปุ่มสำคัญกดได้ตลอดเวลา และจัดวางให้สายตาอยู่ตรงจุดที่ต้องดูระหว่างงานสด 🎬

## ✨ มีอะไรใหม่ (What's New)

- **จัดวางหน้าจอใหม่ทั้งหมด (Console Layout):** ปรับโครงสร้างใหม่หมด **Media Queue ย้ายมาอยู่แถบด้านซ้ายแบบตรึงหน้าจอ** เลื่อนหน้าจอยังไงคิวก็ยังอยู่ ส่วนแผง Hold / Live จัดเป็น 2 คอลัมน์อยู่ด้านขวา ไม่ต้องเลื่อนขึ้นลงหาคิวกลางงานอีกต่อไป
- **แถบหัวจอแบบตรึง พร้อมเลือกจอปลายทาง:** ย้ายตัวเลือกจอ Display ขึ้นไปไว้บน **แถบหัวจอที่ตรึงไว้ตลอด** สลับจอโปรเจกเตอร์ได้ทุกเมื่อโดยไม่ต้องเลื่อนกลับขึ้นไปบนสุด แถมมีข้อความเตือนว่ากด `?` เพื่อดูคีย์ลัดได้
- **แถบปุ่มฉุกเฉินขนาดกะทัดรัด:** ปุ่ม `🛑 Emergency Cut to Standby` ย้ายไปอยู่ใน**แถบบาง ๆ ตรึงไว้ที่ขอบล่างจอ** กดได้ทันทีตลอดเวลา แต่ไม่กินพื้นที่จอเหมือนเดิม พร้อมโชว์คีย์ลัด `B` ติดอยู่บนปุ่มเลย
- **แยกสี Hold กับ Live ชัดเจน:** ฝั่ง Hold / Standby ใช้กรอบ **สีเหลืองอำพัน** ส่วน Live Controls ใช้ **สีแดง** เหลือบตาเดียวก็รู้ว่ากำลังจับฝั่งไหนอยู่ ช่วยได้มากตอนต้องทำงานเร็ว ๆ ใต้ความกดดัน
- **ไฟสถานะ LIVE & ARMED แบบกะพริบ:** ป้าย `● LIVE`, `● ON AIR` และ `● ARMED` จะ **กะพริบเบา ๆ** และกรอบ Live Preview มี **ขอบเรืองแสงสีแดง** ล้อมอยู่ มองจากอีกฝั่งห้องก็รู้ว่ากำลังออกจออยู่
- **รวมทุกอย่างไว้ในกรอบเดียว (Integrated Live Frame):** ตัวเลือก Keep Audio / Loop / Fade ย้ายไปอยู่**แถบด้านบน**ของจอ Live และแถบ Seek + เสียงอยู่**ด้านล่างของกรอบเดียวกัน** ทุกอย่างที่เกี่ยวกับคลิปที่กำลังออกจอรวมอยู่ในแผงเดียวจบ
- **ขนาด Live Preview พอดีจอมากขึ้น:** เลิกใช้กรอบ 16:9 ตายตัวที่ดันเนื้อหาตกขอบจอ เปลี่ยนมาปรับตามความสูงหน้าต่าง (มีขนาดต่ำสุด-สูงสุดกำกับ) ทำให้เห็นแผงควบคุมครบทั้งหมดในหน้าจอเดียว
- **โซนลากวางไฟล์ที่ชัดเจนขึ้น:** หน้าจอตอนลากไฟล์เข้ามาใหญ่และชัดขึ้น มีพื้นหลังเบลอ พร้อม **แสดงรายชื่อนามสกุลไฟล์ที่รองรับทั้งหมด** ไว้บนหน้าจอเลย ไม่ต้องเดาว่าแอปรับไฟล์อะไรได้บ้าง
- **เก็บรายละเอียดครบทุกจุด:** พื้นหลังไล่เฉดสีเข้มสไตล์ห้องคุม, การ์ดหน้าตาเดียวกันทั้งแอปพร้อมเงานุ่ม ๆ, ปุ่มไล่สีที่มีจังหวะตอบสนองตอนกด, **แถบเลื่อนบางสีเข้ม** และหน้าต่างคีย์ลัดที่จัดเรียงปุ่มให้อ่านง่ายขึ้น
- **รองรับการใช้คีย์บอร์ดเต็มรูปแบบ:** ปุ่ม ช่องเลือก และช่องกรอกต่าง ๆ จะขึ้น **ขอบไฮไลต์สีฟ้า** เมื่อกด Tab มาถึง ใช้งานทั้งแผงได้โดยไม่ต้องแตะเมาส์
- **ข้อความคิวว่างที่บอกวิธีใช้:** เมื่อคิวว่างจะบอกเลยว่าต้องทำอะไร — *"Queue is empty — drag files here or click Add"*

> ℹ️ **หมายเหตุ:** เวอร์ชันนี้เป็นการอัปเดตด้านหน้าตาและการใช้งาน (UI/UX) ปุ่ม คีย์ลัด และการทำงานทั้งหมดจาก v1.2.0 ยังเหมือนเดิมทุกประการ ไม่มีการตัดหรือย้ายคีย์ลัดใด ๆ

## 📦 วิธีการติดตั้งและการใช้งาน
เวอร์ชันนี้ยังคงเป็นแบบ Portable สำหรับ Windows โหลดปุ๊บใช้ได้ปั๊บ ไม่ต้องกดติดตั้งลงเครื่อง!

1. ดาวน์โหลดไฟล์ `Projector.Control-win32-x64.rar` จากด้านล่าง
2. แตกไฟล์ RAR (Extract) ไปไว้ในโฟลเดอร์ที่คุณต้องการ
3. เข้าไปในโฟลเดอร์แล้วดับเบิ้ลคลิกเปิดไฟล์ `Projector Control.exe` เพื่อใช้งานได้ทันที

> **หมายเหตุ:** รองรับการทำงานบน Windows 64-bit เท่านั้น
