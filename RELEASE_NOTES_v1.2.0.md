# Projector Control v1.2.0: Power User & Workflow Edition

🚀 **Projector Control Update (v1.2.0) - Power User Features Edition**
We are excited to release the latest update for Projector Control! 🎉 Take your control workflow to the next level with our new keyboard-driven, drag-and-drop, and live transport features designed for professional operators.

## ✨ What's New

- **Full Keyboard Shortcuts:** Drive the entire control panel from the keyboard! `Space` to Play/Pause, `Enter` to Send to Live, `S` to ARM, `B` for Emergency Cut to Standby, `↑`/`↓` to navigate the queue, `Delete` to remove an item, `L` to toggle Loop, `K` to toggle Keep Audio, and `?` to open the on-screen shortcut helper.
- **Drag & Drop to Queue:** Drop media files (`.mp4`, `.mkv`, `.mov`, `.webm`, `.mp3`, `.wav`, `.png`, `.jpg`, `.gif`, `.webp`) directly onto the Control Panel. A big blue overlay confirms the drop zone, and files are added to the Media Queue instantly — no more browsing dialogs in the middle of a show!
- **Drag-to-Reorder Queue:** Reorder items in your Media Queue by simply dragging them up or down. Each row now shows a `⋮⋮` grip handle and a blue ring highlight while dragging, so the order is always under your control.
- **Live Transport Bar:** A brand new transport panel beneath the live preview gives you frame-accurate control of the on-screen video — full **seek bar**, current time / duration display, and a dedicated **volume slider** for fine-tuning audio without leaving the panel.
- **ARMED Status Indicator:** Whenever a file is queued in Hold / Standby, a bright yellow **`ARMED`** badge now appears so you'll never accidentally cut to a stale slide again. Confidence at a glance!
- **Bigger, Resizable Control Panel:** The Control Panel now opens at **1200×800** (up from 900×700) with a minimum size limit so the layout never breaks. The interface is also fully **responsive** — switching to vertical stacking on narrower windows.
- **UI Refresh with Tailwind CSS:** The entire control panel has been rebuilt on **Tailwind CSS**, replacing the legacy stylesheet. Result: cleaner visuals, consistent spacing, and smoother hover states across the app.
- **Display Window Fullscreen Hotkeys:** On the projector display window itself, press `F11` to toggle fullscreen and `Esc` to exit fullscreen — handy for quick recovery during live events.
- **Settings Persistence:** Your preferences (Fade Time, Loop, Keep Audio, etc.) now persist across sessions, so the app opens exactly the way you left it.
- Performance improvements and code cleanup on the Playlist component for even more stable queue management.

## 📦 Installation & Usage
This release remains a Portable Windows Build. No installation required!

1. Download the `Projector.Control-win32-x64.rar` file below.
2. Extract the `.rar` file to your desired folder.
3. Open the folder and double-click `Projector Control.exe` to run the application.

> **Note:** This application requires Windows 64-bit to run.

---

## 🇹🇭 Thai Version

🚀 **Projector Control (v1.2.0) - อัปเดตสายโปร ใช้คีย์บอร์ดล้วน**
เปิดตัวอัปเดตใหม่ล่าสุดสำหรับแอปพลิเคชัน Projector Control 🎉 ยกระดับการคุมงานของคุณให้เร็วและแม่นยำเหมือนมืออาชีพ ด้วยฟีเจอร์ใหม่ที่เน้นการใช้งานด้วยคีย์บอร์ด ลากวาง และคุมจอสดได้แม่นทุกวินาที!

## ✨ มีอะไรใหม่ (What's New)

- **คีย์ลัดครบชุด (Full Keyboard Shortcuts):** คุมแผงควบคุมทั้งหมดจากคีย์บอร์ดได้แล้ว! `Space` เล่น/หยุด, `Enter` ส่งขึ้นจอ Live, `S` ARM เข้า Hold, `B` ตัดเข้า Standby ฉุกเฉิน, `↑`/`↓` เลื่อนรายการในคิว, `Delete` ลบรายการ, `L` เปิด/ปิด Loop, `K` เปิด/ปิด Keep Audio และ `?` เพื่อเปิดดูตารางคีย์ลัดได้ทันทีบนหน้าจอ
- **ลากไฟล์ใส่คิวได้เลย (Drag & Drop):** ลากไฟล์มีเดีย (`.mp4`, `.mkv`, `.mov`, `.webm`, `.mp3`, `.wav`, `.png`, `.jpg`, `.gif`, `.webp`) จาก File Explorer มาวางบนหน้า Control Panel ได้โดยตรง มีแถบสีน้ำเงินใหญ่ขึ้นมายืนยันโซนรับไฟล์ แล้วระบบจะใส่เข้าคิว (Media Queue) ให้อัตโนมัติ ไม่ต้องเปิดหน้าต่างเลือกไฟล์กลางงานอีกต่อไป!
- **จัดเรียงคิวด้วยการลาก (Drag-to-Reorder):** จัดลำดับ Media Queue ใหม่ได้ง่าย ๆ แค่ลากแต่ละรายการขึ้น-ลง มีไอคอน `⋮⋮` บอกจุดจับ และเส้นน้ำเงินไฮไลต์ตอนลาก ทำให้คุณคุมลำดับการเล่นได้ตามใจ
- **แถบควบคุมขณะออกจอ (Live Transport Bar):** แผงควบคุมใหม่ใต้พรีวิวจอ Live ให้คุณคุมวิดีโอได้แม่นทุกวินาที — มีทั้ง **แถบ Seek** พร้อมตัวเลขเวลาและความยาวรวม และ **สไลเดอร์ปรับเสียง** แยกต่างหาก ปรับละเอียดได้โดยไม่ต้องออกจากหน้านี้
- **ไฟแจ้งเตือนสถานะ ARMED:** เมื่อมีไฟล์รอใน Hold / Standby จะมีป้ายสีเหลือง **`ARMED`** โผล่ขึ้นเตือนทันที ป้องกันการตัดสไลด์ผิดในงานจริง มั่นใจได้แค่เหลือบมอง!
- **Control Panel ใหญ่ขึ้นและปรับขนาดได้:** หน้า Control Panel ขยายเป็น **1200×800** (จาก 900×700) พร้อมจำกัดขนาดต่ำสุดไม่ให้ layout เพี้ยน และยัง **ปรับตามขนาดหน้าต่างอัตโนมัติ** — บีบหน้าจอแคบ ๆ จะเรียงเป็นแนวตั้งให้เอง
- **ปรับโฉม UI ใหม่ด้วย Tailwind CSS:** ปรับหน้าตาแผงควบคุมใหม่ทั้งหมดให้ใช้ **Tailwind CSS** แทน CSS แบบเดิม ผลลัพธ์คือหน้าตาสะอาดขึ้น ระยะห่างคงเส้นคงวา และเอฟเฟกต์ hover เนียนตาขึ้นทั่วทั้งแอป
- **คีย์ลัด Fullscreen บนจอ Display:** บนหน้าต่าง Display ของโปรเจกเตอร์ กด `F11` เพื่อสลับเต็มจอ และ `Esc` เพื่อออกจากเต็มจอ ใช้กู้สถานการณ์ระหว่างงานสดได้รวดเร็ว
- **จำค่าตั้งให้อัตโนมัติ (Settings Persistence):** ค่าต่าง ๆ (Fade Time, Loop, Keep Audio ฯลฯ) จะถูกบันทึกไว้ระหว่างการเปิดใช้งาน เปิดแอปครั้งหน้ามาก็ใช้ค่าเดิมต่อได้เลย ไม่ต้องตั้งใหม่
- ปรับปรุงประสิทธิภาพและจัดระเบียบโค้ดของ Playlist Component ให้การจัดการคิวเสถียรยิ่งขึ้น

## 📦 วิธีการติดตั้งและการใช้งาน
เวอร์ชันนี้ยังคงเป็นแบบ Portable สำหรับ Windows โหลดปุ๊บใช้ได้ปั๊บ ไม่ต้องกดติดตั้งลงเครื่อง!

1. ดาวน์โหลดไฟล์ `Projector.Control-win32-x64.rar` จากด้านล่าง
2. แตกไฟล์ RAR (Extract) ไปไว้ในโฟลเดอร์ที่คุณต้องการ
3. เข้าไปในโฟลเดอร์แล้วดับเบิ้ลคลิกเปิดไฟล์ `Projector Control.exe` เพื่อใช้งานได้ทันที

> **หมายเหตุ:** รองรับการทำงานบน Windows 64-bit เท่านั้น
